let testArt = fetchOrder(order)
listenForFormSubmit(order);
listenForFormSubmit(orderID);

let orderId;
console.log(orderId);

document.getElementById('main-test').innerHTML = 'Numéro du commande : ' + orderId;
